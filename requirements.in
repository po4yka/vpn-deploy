ansible-core==2.20.5
ansible-lint==26.4.0
jmespath==1.1.0
molecule==26.4.0
molecule-plugins[docker]==25.8.12
yamllint==1.38.0

# Used by validators in CI (scripts/check-secrets-coverage.py +
# scripts/check-templates-render.py +
# scripts/validate-secrets.py — which uses the jsonschema pulled
# transitively by ansible-lint, no explicit pin here).
PyYAML==6.0.3
Jinja2==3.1.6

# pytest is NOT pinned here — it's not part of the deploy runtime;
# the ci-fast job installs pytest separately with an explicit
# version, so Scorecard's pinned-dependencies rule still passes.
