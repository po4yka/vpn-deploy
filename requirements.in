ansible-core==2.20.5
ansible-lint==26.4.0
jmespath==1.1.0
molecule==26.4.0
molecule-plugins[docker]==25.8.12
yamllint==1.38.0

# Used by validators in CI (scripts/check-secrets-coverage.py +
# scripts/check-templates-render.py).
PyYAML==6.0.3
Jinja2==3.1.6

# Test layer (pytest unit + snapshot + terraform); molecule already
# pulls in pytest transitively but we pin the floor here.
pytest==8.4.0
jsonschema==4.23.0
