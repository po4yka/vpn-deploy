# Vultr provider — stub

Not implemented in v1. Shape mirrors `providers/upcloud/`. Use
`vultr_instance`, `vultr_firewall_group`, `vultr_firewall_rule`,
`vultr_ssh_key`. Outputs must match the UpCloud module so the inventory
render script works unchanged.
